#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '() :silent t)
  )

(defpackage :ros.script.aliya.3825906675
  (:use :cl))
(in-package :ros.script.aliya.3825906675)

(defun main (&rest argv)
  (declare (ignorable argv))
  (format t "~a" "Hello Aliya!!!"))
;;; vim: set ft=lisp lisp:
